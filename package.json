{
  "name": "switch-eshop-radar",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "crawler",
    "web"
  ],
  "scripts": {
    "build:api": "mkdir -p api && cp data/games.json api/ && cp data/latest.json api/",
    "build:web": "npm run -w @eshop/web build && cp -r api web/dist/",
    "build": "npm run build:api && npm run build:web",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts,.vue",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "prettier": "^3.4.0"
  }
}
